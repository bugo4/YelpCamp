<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><% camp.title %> </title>
</head>
<body>
    <h1><%= camp.title %> </h1>
    <h2><%= camp.description %> </h2>
    <h3>Made by <%= camp.author.username %> </h3>
    <!-- Edit camp -->
    <h3><a href="/camps/edit/<%= camp._id %>">Edit</a></h3>
    <!-- Delete camp -->
    <form action="/camps/<%= camp._id %>?_method=DELETE" method="POST">
        <button>DELETE CAMPGROUND😥</button>
    </form>
    <!-- Add a review -->
    <h3>
        Add a review!
    </h3>
    <form action="/camps/<%= camp._id %>/reviews" method="POST">
        <label for="review[stars]">Stars:</label>
        <br>
        <input type="range" id="review[stars]" name="review[stars]"
         min="1" max="5">
         <br>
         <label for="review[body]">Type your review here:</label><br>
        <textarea type="text" id="review[body]" name="review[body]" cols="30" required></textarea>
        <br> <button>Send Review &star;</button>
    </form>
    <h2>
        Reviews from visitors:
    </h2>
    <% for (let review of camp.reviews) { %>
        <h2> 
            <%- ("&star;").repeat(review.stars) %>
        </h2>
        <p>
            <%= review.body %>
        </p>
        
        <form action="/camps/<%= camp._id %>/reviews/<%= review._id %>/?_method=delete" method="post">
            <button>Delete Review</button>
        </form>
    <% } %>  
</body>
</html>